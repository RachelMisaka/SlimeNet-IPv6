# SlimeNet-IPv6 的兼容层实现

> 本文件承接《底层实现》与《安全层实现》的角色划分，修订《readme》3.6 的兼容层设计。旧分类把可编程设备一律当作"虚拟节点经隧道接入"，本文件不再采用：可编程设备中满足 mesh 准入条件的直接成为标准节点，支持标准 SLAAC 与 RA 的设备作为末端零配置接入，兼容层只处理其余能力鸿沟。

## 1. 角色再划分

设备接入网络时按准入条件判定角色，不再沿用《readme》3.6 的 A/B 分类。判定依据是一条硬性准入条件链：能转发数据包、能通过软件修改路由表的（《底层实现》2.3），成为标准节点；否则支持标准 IPv6 SLAAC 与 RA 的，作为末端设备零配置接入；两者都不满足的，进入兼容层，由入口节点代管。

旧 A/B 分类过时的原因有两点。其一，A 类设备（树莓派、旧 PC、软路由、双网口主机）绝大多数满足标准节点的准入条件——能跑守护进程、能改路由表、能转发，直接承担 mesh 业务即可，无需虚拟节点与软件预路由那套中间机制。其二，B 类中具备 IPv6 协议栈的新设备走标准 RA 即完成接入，无需兼容层代管。真正剩下的，是无 IPv6 栈的存量设备与固定固件的哑设备。

### 1.1. 标准节点

准入条件为能转发、能通过软件修改路由表。标准节点运行 slimenetd（《用户层实现》），在广播中声明 mesh 能力并携带证书指纹（《安全层实现》1.1），承担 mesh 业务：占本层 256 成员名额，进入双路由表，经两阶段验证后成为可信转发成员。这一档涵盖了旧 A 类的主体。

### 1.2. 末端设备

准入条件为支持标准 IPv6 SLAAC 与 RA，接入过程零配置：入口节点在接口上周期性发送 RA，宣告 ULA 前缀与默认网关，设备经 SLAAC 生成地址、经 DAD 确认无冲突，即完成接入。末端设备不声明 mesh 能力，不持证书、不广播拓扑、不参与时钟同步，信任由接入的边缘节点代持，不占 256 名额，不进双路由表（《安全层实现》1.1）。现代操作系统默认满足这一条件，这是新设备的默认接入方式，量大而无需任何协议工作。

### 1.3. 兼容层

达不到上述两项准入条件的设备进入兼容层，主体是两类：无 IPv6 栈的存量软件设备，与不能改动任何配置的固定固件哑设备。兼容层不再按设备可编程能力划分类型，而按入口节点需要代管的职能划分，见第 2 节。兼容设备的共同点是：都不参与 SlimeNet 路由协议、不持证书，被整体视为入口节点的终端。

## 2. 代管方式

兼容设备按能力分三种代管方式，均由入口节点承担。

### 2.1. 桥接代管的哑设备

固定固件、不能改动任何配置、依赖 L2 可见性的设备（需要广播与组播发现，如 mDNS、SSDP、ARP），入口节点用网桥或 VXLAN 在本地构建虚拟二层，把设备所在物理网段并入入口的虚拟子网，设备如同插在普通交换机上。DHCP 分配地址、默认网关指向入口、DNS 代理，全部由入口代管，设备出厂即插即用。L2 桥接保证发现类广播协议照常工作，哑设备无需感知任何 SlimeNet 语义。

### 2.2. 翻译代管的 IPv4 存量设备

无 IPv6 协议栈的存量软件设备，入口节点以 NAT64 或 4to6 翻译其流量，配以 DHCP 与 DNS 代理。设备所见仍是熟悉的 IPv4 内网，流量经入口翻入 ULA 域（《readme》2.5）。这一档替代旧 A 类中不能成为标准节点的残余部分——它们不能改路由表、不能转发，但本身只是缺 IPv6 能力，翻译即可，无需隧道与虚拟身份那套机制。

### 2.3. 远程软件设备

物理上不处于入口所在广播域的软件设备，无法经 SLAAC 或网桥直接接入，入口节点与其建立标准隧道（WireGuard 等）作为一条直连虚拟链路。设备经隧道获得入口代管的地址，等同末端接入；隧道仅提供链路承载，不引入任何 SlimeNet 协议子集。跨公网与 NAT 穿越也由此承载，呼应《用户层实现》2.3 的兼容层兜底定位。

## 3. 标准入口转发

入口节点作为兼容层与核心网络的唯一桥接点，承担全部网络职能。其一，终结所有接入方式——L2 桥接与隧道，作为兼容子网的默认网关。其二，协议翻译——NAT64、4to6，把兼容设备的流量翻进 ULA 域。其三，地址服务——DHCP、DHCPv6、DNS 代理，弥补老设备缺 IPv6 或缺自动配置能力的空缺。其四，信任代持——兼容设备不经过两阶段准入（《安全层实现》1.2），兼容流量视为入口节点的流量，按 L0 全明文处理（《readme》4.13），加密与否不改变核心网络的安全姿态。核心网络因此无需为兼容设备降低任何标准。

## 4. 与核心网络的边界

兼容设备不进入核心 LSDB，不参与 SPF 计算，不占 256 名额。其可达性只表现为入口节点宣告的一条虚拟子网路由，按《底层实现》第 3 节打包成一条汇总，不泄露兼容层内部细节。兼容流量一律视为入口节点的流量，信任由入口代持。能力鸿沟由此被挡在网络边缘：老设备永远只对接一个能力完整、可信的入口节点，核心网络保持统一的路由协议与信任模型。

---

《readme》3.6 原"虚拟 SlimeNet 节点 + 软件预路由"机制整体撤销。其覆盖的设备分别被标准节点、末端设备与翻译代管吸收；软件预路由与虚拟身份所依赖的受限拓扑表，在标准节点上由 slimenetd 全量承担，不再存在降级形态。
