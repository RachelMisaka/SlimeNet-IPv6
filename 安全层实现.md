# SlimeNet-IPv6 的安全层实现

> 承接《底层实现》第 1 节：新设备确认地址无误后，按层级设定转入二级程序，即身份验证与路由表建立。本节即该二级程序中的接入验证逻辑。吊销、持久化、跨组织信任等其余安全机制后续补充。

## 1. 新设备接入验证逻辑

### 1.1. 角色判定

新设备接入后先判定角色，决定是否进入验证流程。末端设备不声明 mesh 能力，不持证书、不广播拓扑，直接经 SLAAC 与默认网关接入，信任由接入的边缘节点代持，全程零参与，不进入验证流程。mesh 节点在广播中声明 mesh 能力并携带证书指纹，进入下方两阶段验证。末端流量一律视为其接入边缘节点的流量，不占 256 成员名额，不进双路由表。

### 1.2. 两阶段验证（mesh 节点）

验证的核心目标是让未验证设备可被看见但不可承载流量。落到双路由表上，就是只出现在链路路由表，不出现在安全路由表。

#### 阶段一：广播登记（明文，入链路路由表）

新节点通过 L3 UDP 广播声明自身，携带 Router ID、邻居列表、证书指纹与时间戳载荷，广播间隔与 Hello 一致（有线 1 秒、无线 10 秒）。邻居收到后写入链路路由表，标记为 UNVERIFIED，启动 30 秒验证计时器。广播不验签：链路表不承载流量，明文广播只造成可见性污染，不构成截流；伪造者只能出现在链路表，无法进入安全表。

#### 阶段二：证书验证（验签，入安全路由表）

新节点一次性单发证书，不在每个广播里携带，避免 MTU 超限。证书包含 Ed25519 公钥、签名、绑定的 IPv6 地址、角色与所属区域 /56 前缀哈希。邻居对其执行校验链，任一环不过即拒绝。

校验链共五环。其一，证书内 IPv6 地址须与广播声明地址一致，防地址欺诈。其二，用本地 Trust Store 中已导入的公钥验证证书签名。其三，证书区域哈希须与本地 /56 一致，跨组织则转入 PENDING_APPROVAL，等待管理员执行 `trust anchor import`。其四，时间校验：证书须落在 NotBefore/NotAfter 窗口内，容忍 ±5 分钟，且广播时间戳连续 3 个周期无剧烈抖动；时间未收敛时按 1.5 降级，存在日晷锚时叠加 1.6 的绝对可信拒证。其五，地址所有权：该地址在安全路由表无 TRUSTED 占用者，已有则拒绝并标记 BLOCKED。

全部通过即写入安全路由表，成为可承载流量的转发成员。证书通过但时间偏差超过 ±1 秒的记为 TRUSTED_PENDING_TIME，属中间态，只在链路表可见并打标记，时钟收敛后自动升入安全表。验签失败或 30 秒超时的保持 UNVERIFIED，仅在链路表可见，记录日志。

安全路由表的写入只有一条途径：证书验签通过、地址绑定一致、无所有权冲突。未验签的广播永远进不了安全表，任何伪造身份或伪造网关都无法承载流量。

双归属节点是《底层实现》同级边界形态一的设备。它在 A、B 两侧各走一遍本流程，各用本层证书身份，两侧都入安全表后才可跨网承载流量；任一侧验签不过，就只在该侧保持可见不可转发。

### 1.3. 信任引导（第一批节点）

全新区域无任何信任锚时安全表永远为空，验证流程自锁。验证之前必须先有信任锚，有三种注入方式。预置即在部署时预装本区域信任锚，推荐 L1 使用；带外导入由管理员通过 CLI 执行 `slimenet trust anchor import` 导入公钥并指定首个可信节点；带外确认则由已信任节点以物理按钮或一次性口令确认新节点。三种方式等价于信任的第一次注入，注入后正常验证流程即可运作。

### 1.4. 运行层级对验证的影响

运行层级决定阶段二是否执行，阶段一照常广播登记。L0 全明文模式下阶段二直接跳过，安全表复制链路表，机制关闭，末端零参与设备因此可正常通信。L1 自动签发模式下区域 CA 自动签发 24 小时临时证书，验签通过即 TRUSTED。L2 零信任模式下两阶段与带外确认强制执行，UNVERIFIED 一律不进安全表。

### 1.5. 时间降级

验证中的时间依赖（证书时效、时间戳稳定、防重放）在时钟不可靠时按序降级。时钟未收敛、偏差超阈值时，证书时效窗口放宽至 ±5 分钟，即 readme 3.3 阈值体系最宽档。冷启动无绝对时间、处于 1970 基准时，跳过绝对时效，改用证书序列号相对先后校验，直至有节点经外部渠道获取绝对时间并广播。降级只放宽时效判定口径，安全表仍按验签结果写入——信任不因时间降级而凭空获得。

### 1.6. 绝对可信时间拒证（防时间欺骗）

物理观测低漂移设备（日晷、月相、星历、六分仪，见《分布式时钟实现》1.11）只要网络时间未脱离其声明的可信区间，其观测读数即为绝对可信时间。物理现实（太阳、月亮、恒星位置）无法被伪造，证书的时间就无从欺骗。

机制的生效条件是本节点的物理观测锚有效（CA 背书、证书未过期、读数时间新），且网络时钟与其偏差在声明的系统误差内、未触发漂移超标告警。绝对可信区间为观测读数加减声明系统误差，区间内的时间视为物理真实。对任何证书或声明的时效校验，其声称的时间若与绝对可信区间物理冲突，即直接拒绝，判为时间欺骗，不依赖网络时钟。典型场景是攻击者重放过期证书、或伪造"未来"签发时间绕过有效期——物理观测区间证明该时间物理不可能，即使网络时间被污染也无法实现有效攻击。网络时间漂移、超出可信区间时，触发漂移告警与重校准，物理观测锚仍可信，网络侧失准按 1.5 降级口径处理。绝对可信拒证在区间内始终有效。观测锚自身的读数另受《分布式时钟实现》1.11 的时间间隔验证监督，伪造读数无法通过，杜绝污染绝对可信判定。

物理观测锚是 readme 3.4 TAM 的持续可用的证书化实现。TAM 靠管理员手配表达式（《分布式时钟实现》3），观测锚自动提供物理真实时间窗口且经 CA 背书。

例如某证书 NotBefore 声称 2027-01-01，而日晷绝对可信区间为 2026-08-28 12:00 ±15 分钟，则该证书时间物理不可能，直接拒绝。

## 2. 信任状态与连通性策略

落到双路由表（《底层实现》2）上的数据面约束有两条核心策略。

### 2.1. 未验证设备：可收发包，但不经过它路由

未验证设备可作为目的地址被交付，包在到达它之前的每一跳都走安全路由表，仅最后一跳由直连的 TRUSTED 邻居直接交付给它。它也可作为源地址发包，包交给其直连的 TRUSTED 邻居，此后全程走安全路由表转发。但未验证设备不在安全路由表中，任何节点查表都不会选它作下一跳，它永远成不了中转转发跳。普通传输只在安全路由上传输，因此路径全部由 TRUSTED 节点构成。

未验证设备是合法的通信端点，但不是路径的一部分。这与末端零参与一脉相承：端点无需信任，路径必须可信。

### 2.2. 吊销设备：直接拒绝所有连接

CRL 命中的设备（私钥泄露、CA 被攻破后的强制撤销，见 readme 4.11）被直接拒绝。不处理其任何 Hello/LSP，从链路表与安全表同时移除，不写入拓扑。数据面上，源地址为它的包与目的地址为它的包一律丢弃，不转发不交付。任何连接或流量意图直接拒绝，不建立邻居与拓扑记录，仅记入安全告警日志。

吊销设备在网络中物理通、逻辑全断，比 BLOCKED（IP 抢占惩罚，30 分钟后可重新评估）更彻底、不可逆。除非重新签发并重新走 1.2 两阶段验证，否则不恢复。

### 2.3. 全状态连通性矩阵

综合各状态的连通性如下。TRUSTED 在链路表与安全表均存在，可作转发中转与通信端点，是完整成员。TRUSTED_PENDING_TIME 在链路表打标、不在安全表，不作转发中转但可作通信端点，时钟收敛后自动升入安全表。UNVERIFIED 在链路表、不在安全表，不可作转发中转，但可收发包，是合法的通信端点。PENDING_APPROVAL 在链路表打标、不在安全表，不作转发中转，默认不作通信端点，等管理员 `trust anchor import`。BLOCKED 的 LSP 被忽略，不在安全表，不可转发不可通信，30 分钟惩罚期后可重评估。吊销设备被移出两表，进出全部拒绝，恢复需要持有效证书并且重启物理链路层。

## 3. 个人 GPG 认证（P2P 端到端）

双轨信任模型的第二条轨（readme 4.2 / 4.9）是个人证书与组织证书完全隔离，专用于终端间端到端认证与加密。组织控制得了网络，控制不了个人身份。

### 3.1. 身份与签发

每个用户生成自己的 Ed25519 密钥对，自签名形成个人身份证书，OpenPGP 身份 UID 风格。个人私钥只在用户自己手里，不依赖任何组织 CA、不依赖任何网络管理员。管理员持有的是组织 CA 私钥，没有用户个人私钥，无论本方还是对方网络的 admin 都无法冒充用户身份、无法伪造用户签名。管理员最多吊销组织证书把设备踢出网络，个人身份独立存活。

### 3.2. 信任建立（P2P）

首次交互时双方互换个人证书，显示指纹，由用户手动确认是否信任。用户对信任的友人证书签名后广播，形成去中心化信任传递；该传递不影响路由决策，只作用于应用层端到端信任。脱离组织网络后 P2P 互信依然成立，个人证书的生命周期不依赖网络存在。

### 3.3. 与组织证书的边界（防横向渗透）

控制面（路由）默认绑定组织证书形态，个人证书绝不用于 mesh 节点验证、路由协议认证、安全路由表写入；1.2 的验证流程使用组织 CA 形态的信任锚，无 CA 组织的替代形态见第 4 章。数据面由应用层使用，终端间端到端加密与认证走个人证书。即使管理员控制了整个网络，可踢人、可撤销组织证书、可查看全部流量，也无法以终端用户身份向其他用户发起端到端认证，个人层身份不受网络层控制。

### 3.4. 密钥保管与恢复

私钥丢失后用旧私钥签发吊销；无旧私钥则走社交渠道更新指纹（见 readme 4.11）。建议个人私钥离线备份，不与设备同机单点保存。

## 4. 控制面路由认证的信任锚形态（无 CA 组织）

1.2 阶段二第 2 环"信任锚验签"的信任锚不只有组织 CA 一种形态。对追求完全无 CA 的组织，可按规模选择替代方案。

### 4.1. 三种形态对比

三种形态按信任锚、认证方式、带外交换量、身份粒度与撤销能力区分。组织 CA 形态是默认选择，以 CA 公钥为信任锚，走证书链验签，带外交换量与节点数成正比（CA 签一次、预置 CA 公钥），身份粒度细，可集中撤销（CRL），适用于有信任基础设施的环境。公钥全交换形态以各节点裸公钥直接互信，广播签名对源公钥验签，带外交换量随节点数平方增长，身份粒度细，但撤销困难，只能经社交传播并重新全交换，适用于无 CA 的小规模网络。PSK 预共享密钥形态以单条共享密钥为信任锚，广播 HMAC 验签，带外交换量恒定、分发一次，但只认证本子网成员而不验证身份，无撤销粒度，泄露即全塌，适用于物理高度可控、最简部署的环境，对应 readme 4.13 L0/L1 的简化认证面。

### 4.2. 公钥全交换（零 CA、细粒度）

信任锚集是本子网所有成员的裸公钥。各节点带外逐对交换后直接互验广播签名，无证书链、无 CA 签发。N 个节点需两两带外交互，交换量随节点数平方增长；每层 256 上限，单层最多 32640 对。规模小（个人圈子、临时灾备网）可接受，规模一大即不可行。撤销无 CRL 可用，只能经社交渠道传播某人不可信，并重新全交换受影响部分。

### 4.3. PSK（零 CA、最简）

整子网共享一条密钥，广播 HMAC 认证成员身份——不验证你是谁，只验证你是不是本网成员。交换量恒定、部署最简，但单节点泄露即全网路由认证失效，可伪造任何成员广播，且无吊销粒度。

### 4.4. 与个人 GPG 的关系

以上三种都是控制面（路由）认证形态，个人 GPG 永远走数据面端到端，不参与其中任何形态。控制面选哪种信任锚，都不影响个人证书的独立性与管理员不可伪造性。

## 5. CA 链与权重

承接 4.1 的组织 CA 形态，多个组织 CA 之间的互信有两种方式，均非强制。层级链由大 CA 对小 CA 签发包含权重的签名，权重标明被授权 CA 的可信程度，信任沿链向下传播；下级 CA 的节点证书、路由宣告与汇总签名据此折算可信度，权重越高越可信。交叉签名则是两个对等 CA 互相签名，互信不依赖任何大 CA，等同 readme 4.8 的 Web of Trust。组织可任选其一或两者并存；权重只调整可信程度，不改变信任的锚定原则。

### 5.1. 本地权重与乘法链

节点对直接认识的 CA 设置本地权重，作为该信任锚的强度，随 `trust anchor import` 一并录入。对经上级 CA 认证、但本地没有权重记录的 CA，其权重按乘法链计算：本级权重等于上级权重乘以上级签名中携带的权重，多级认证沿链连乘。信任因此沿链逐级衰减，距本地锚越远的 CA 权重越低；本地直接设置的权重不受衰减影响，充当链条的锚值。

### 5.2. 本地可信度阈值

节点可本地设置可信度阈值，作为把权重折算成信任等级的判据。算出的 CA 权重高于阈值即视为可信，低于阈值则降档对待；阈值既供操作者人工裁决，也供程序自动决策，如自动接受高于阈值的跨组织信任、把低于阈值的挂起待审。阈值只在本地方配置，调整它不改变对端 CA 的权重，只改变本地对该权重的判定口径。
