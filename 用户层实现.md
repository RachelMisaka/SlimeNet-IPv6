# SlimeNet-IPv6 的用户层实现

> 本文件为规划稿，承接《底层实现》2.3：mesh 节点的准入条件只是能转发数据包、能通过软件修改路由表。本规划论证该条件可由一个用户态控制面守护进程配合系统转发满足，无需专用硬件，也无需内核改动。下文先定数据面形态，再分模块列控制面，最后给平台、选型与验证路线。

## 1. 总体形态

SlimeNet 的实现收敛为一个控制面守护进程，本文称 slimenetd，配套一个管理命令行。协议栈的智能部分全部在进程内：发现、拓扑、路由解算、信任、时间同步不触碰内核网络栈的任何专有部分。数据面不做用户态转发，也不封装隧道，而是借用系统自身的能力：slimenetd 把算好的路由写入系统路由表，内核按表转发，接口、地址、邻居解析、转发开关全由操作系统打理。这是 FRRouting、BIRD 一类的成熟形态：用户态控制面加内核转发面，也是《底层实现》2.3"能通过软件修改路由表"的直白落地。

slimenetd 对操作系统只索取三类能力。其一为 UDP 套接字，承载 Hello 广播。其二为系统路由配置接口，Linux 上走 networkd 或 netlink，Windows 上走路由配置 API 与 netsh，用于落表与启停转发。其三为接口配置，用于设定叠加网地址、启用 IPv6 转发、建立网桥等。三种能力均为操作系统标准接口，Windows、Linux、macOS 都有对等实现，程序不依赖任何内核改动。

## 2. 数据面承载

### 2.1. 系统转发

mesh 节点本质是一台普通路由器。同一广播域内的节点，下一跳就是邻居的链路层地址，由系统邻居缓存解析，内核直接按以太网交付，这是 L2 段。跨广播域的节点，靠多网口在每口各属一个广播域，内核按路由表做 L3 转发，这是 L3 段。开启系统的 IPv6 转发开关后，数据报文全程由内核处理，slimenetd 不碰任何数据报文。隧道不在数据面使用，只在兼容层 A 类设备的远程接入等场景出现，且由系统隧道接口承载。

### 2.2. 路由落表

slimenetd 把路由解算的结果通过系统路由配置接口写入内核路由表。写表有三类条目。其一为直连前缀，节点的末端子网由系统按接口地址自动生成。其二为安全路由，SPF 算得的路径逐条落表，下一跳是邻居的链路地址或接口地址。其三为汇总路由，边界节点把上级下发的汇总按《底层实现》第 3 节落成静态路由。落表处即信任过滤的落地处：安全路由表只含 TRUSTED 节点为下一跳的路径，UNVERIFIED 下一跳不落表，内核转发面因此天然不含未验证路径，与《安全层实现》2.1 的约束一致。

### 2.3. 广播与邻接

Hello 仍用 UDP 广播，TTL=1，不跨广播域，遵守《底层实现》的广播隔离。slimenetd 从广播中学得邻居的身份、证书指纹与时间载荷，邻居的链路层地址解析交由系统邻居缓存（NDP）完成，程序不需要与链路层细节纠缠。末端设备直接 L2 接入节点，SLAAC 获址，默认网关指向节点，IPv6 转发由系统承担，信任由节点代持。IPv4 末端与哑设备按《readme》3.6 兼容层处理，在节点上代管 NAT64、DHCP 等职能。

## 3. 控制面模块

### 3.1. 发现与拓扑

邻居发现按《底层实现》第 1 节流程实现。slimenetd 周期性广播 Hello，携带 Router ID、邻居列表、证书指纹与时间戳载荷，间隔有线 1 秒、无线 10 秒。收到 Hello 即写入链路路由表并老化，链路状态库由此增量构建。SPF 在拓扑变化时于进程内重算，结果经 2.2 落进系统路由表。逐级汇总与单向下行天然成立：本层只维护本层全量，上级汇总由边界组件注入并落表。

### 3.2. 双路由表

链路路由表与安全路由表是路由解算的数据来源。链路表记录一切可被看见的邻居，含 UNVERIFIED；安全表只容纳 TRUSTED 节点，写入的唯一途径是验签通过、地址绑定一致、无所有权冲突。两条表都存在进程内，系统路由表只反映安全表，未验证节点可作通信端点但永不成路径。过滤在进程内判定、在落表处执行，两层各司其职。

### 3.3. 安全模块

两阶段验证、信任锚、吊销、运行层级全部按《安全层实现》实现。证书用 Ed25519，由密码学库提供签名与验签，证书与信任锚存于配置目录。阶段一登记入链路表，阶段二验签合格入安全表，TRUSTED_PENDING_TIME 与 PENDING_APPROVAL 等中间态按其定义处理。个人 GPG 轨与组织 CA 轨隔离，控制面只用组织形态的信任锚。密码学实现不自造，一律走经过审计的库。

### 3.4. 时钟模块

分布式时钟按《分布式时钟实现》第 1 节实现：Hello 携带时间载荷，加权平均收敛，阈值滞回，冷启动引导，TAM 表达式判定。时钟与信任联动，时间样本的权重取自身份状态，恶意样本按剔除规则丢弃。时钟模块不依赖任何授时服务，全部在进程内维护。

## 4. 边界与冗余

同级边界按《底层实现》2.2 实现为可配置的两端点组件，一套代码覆盖双归属与点对点两种形态。双归属即一台多网口机器承载两个 mesh 实例，每口各属一个广播域，各占一套时钟与身份，系统按网口路由，这就是系统转发的自然用法。点对点即边界两端点之间的互联，落表时写入对端宣告的汇总路由即可。一台机器可同时承载多条边界，并行配置实现灾备与负载均衡。边界端的信任结算照旧：对端宣告的汇总路由须经本地信任锚验签方可落表。

## 5. 管理面

slimenetd 以配置文件与命令行双入口管理。配置文件声明角色、所属区域前缀、接口与端口、运行层级、信任锚路径、时间与 TAM 设定。命令行提供《安全层实现》与《分布式时钟实现》约定的操作：`trust anchor import`、`clock set`、`clock accept-mask`、状态查询等。状态查询输出本层拓扑图、双路由表、各邻居的信任等级与时间偏差，供网管与排障。渲染脚本 `netjson2dot.py` 可直接消费状态导出的网表。

## 6. 平台与权限

跨平台是用户态实现的直接收益。三平台差异集中在落表与转发开关两处。Linux 上对接 networkd 或 netlink，Windows 上用路由配置 API 与 netsh，macOS 用 routed 与 ifconfig。写系统路由表、启用转发需要管理员权限，程序以普通用户运行时降级为只能维护本机身份与观察拓扑，不能落表。UDP 广播在三个平台无差异，是 Hello 共用的基础。初次运行向导负责申请权限并建立配置目录。

## 7. 技术选型

语言推荐 Go：编译为静态单二进制，Windows、Linux、macOS 直接分发，无运行时依赖；netlink 与路由配置的库生态成熟，并发模型契合控制面的周期任务。备选为 Rust，性能更强但生态与开发速度略逊；工具链已有 Python，但不适合作守护进程。密码学一律用经过审计的库，配置格式用 TOML 或 YAML。核心原则是协议逻辑全部自研，系统与密码学能力全部借库借内核。

## 8. 实施路线与验证

按阶段推进，每阶段有明确验证目标，全部可在单机仿真环境先行。

阶段 0 搭建骨架：UDP 广播、邻居表、系统路由落表、管理命令行，单节点自检通过。

阶段 1 双节点互联：两个进程在同一网段互相发现，信任锚注入后双向 ping 通，末端设备经节点网关入网，数据面全程由系统转发。

阶段 2 多节点与冗余：三节点环形组网，验证 SPF、等价多路径与链路断开后的自动切换。仿真做法是用 Linux 命名空间或容器在单机运行多个实例，各占一个广播域，覆盖大部分拓扑试验而无需硬件。

阶段 3 边界与层级：配置双归属与点对点边界，验证汇总落表、双向透传与信任结算；拔线重连，验证分裂与融合行为。

阶段 4 安全与时钟：用未验证节点与抢占攻击测试信任过滤与地址所有权，验证时间降级链与 TAM 求生模式。

每阶段结束按《readme》5.5 的分阶段部署路径对照，确认与理论设计一致后再进入下一阶段。

---

slimenetd 亦可降级为《readme》3.6 兼容层 A 类设备上的轻量代理形态运行：只维护虚拟子网的受限拓扑与软件预路由，出域流量上交入口节点。同一代码库降级使用，无需单独实现。
