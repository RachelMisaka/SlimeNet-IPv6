# SlimeNet-IPv6 的底层实现

> *SlimeNet 实际上是底层无关的一个拓扑架构，为了便于讨论，我们基于现有的以太网、WiFi 和 TCP/IP 等互联网基础设施进行实现。本实现基于 IPv6。*

重要协议：LLDP（链路层发现协议）。

## 1. SlimeNet 新设备的加入流程

当一个新设备通过有线/无线方式接入时，网卡自动生成一个 fe80::/64 本地地址。新设备会通过 LLDP 向邻居设备广播自身存在，同时发送 RS 请求。邻居设备也会通过 LLDP 确认链路层存活。邻居设备会回复 RA，包含 ULA 前缀，新设备通过 SLAAC 生成全局地址。新设备发送邻居请求，确认地址无冲突。在这里，每个层的逻辑设备数量被限制为 256 个，以限制拓扑复杂度。确认地址无误后，按层级设定转入二级程序：身份验证（参考安全层实现）和路由表建立。

## 2. 路由系统

根据层级设定，符合条件的设备将会加入路由表。路由表分为链路路由和安全路由。每个节点会在 L3 层通过 UDP 广播自身连接的其他节点，使当前层的其他节点能建立拓扑。通过广播方式建立的拓扑为链路路由，只代表链路存在，不保证安全性。根据安全层，通过验证的设备会被加入安全路由。普通传输仅在安全路由上传输。

### 2.1. 路由示例

用一个具体的小网络走一遍，看一个底层节点从"知道路由"到"转发一个包"的全过程。

#### 示例网络

我们有一个底层节点 Rachel。这个节点有很多邻居，在一张 Mesh 网里；这个 mesh 网又和其他 mesh 网有边缘连接。Rachel 所在的网叫做 A 网，A 网的几个边缘位置的节点又同时连接了 B 网。A、B 出于某种原因签了条约，决定对外只表现为 [A,B] 网。与 [A,B] 同级的又有一个 C 网；C 网和 B 网边缘有连接。

现在 Rachel 知道了 C 网中一个朋友的地址，想建立一个连接。Rachel 首先根据广播建立了拓扑表，知道自己在 A、[A,B]、[[A,B],C,D] 内。根据拓扑表，朋友的地址位于 C 网，所以 Rachel 发送了一个包给能连接 C 网的邻居节点，包上带了源头地址和目标地址。邻居也根据路由拓扑表寻找下一个邻居节点……

现在包到了 A|B 边界位置了，边界节点迅速根据 B 内拓扑，找到成本最优路径，并发往第一个邻居节点……

C 网朋友收到了包，开始组织回复。查询拓扑得知，Rachel 的源地址在 [A,B] 内，于是朋友规划了到 [A,B]|C 边缘节点（物理上一个 BC 边缘节点）的最优路径，发包。包到了 [A,B]|C 边缘节点，节点发现这个包应该发往 A；包到了 A|B 边缘节点，节点发现应该发往 Rachel；于是 Rachel 收到了回复。

### 2.2. 同级边界传输设备的实现

同级边界有两种实现。一种是一台机器既属于 A 又属于 B，另一种是 A、B 各出一台机器，中间拉根网线。

#### 2.2.1. 双归属节点

举个例子：一台放在两网之间的软路由，两张网卡分别连接 A 网和 B 网，每个网卡各跑一个独立的 mesh 实例，各自维护一张 A 或 B 的拓扑表。广播域通过 TTL=1 隔离。两张网卡之间绝对不能做二层桥接，必须走转发，否则 A、B 的广播域就会失去隔离，造成严重后果（拓扑崩溃、广播风暴等）。

地址上，它在 A、B 各占一个 IP，对外等于两个节点。信任上，这个节点必须得到两侧 CA 签发的证书，否则无法承担路由业务。转发根据对应子网的拓扑进行路由解算，如 A 发往 B 的包就按照 B 网拓扑进行计算，然后发往邻居；同理，这个操作是对称的，实现双向透传。出于冗余或者别的目的，可以设置多个并行节点，实现灾备和负载均衡。

#### 2.2.2. 点对点互联

另一种实现是 A、B 各出一台机器，X 属于 A、Y 属于 B，中间拉一根点对点网线。网线两端使用 fe80::/64 链路本地地址或协商的独立 IP，不属于 A 也不属于 B，不占两边的 256 名额。这条链路不进行 mesh 广播，只有 X、Y 两个端点。

X 在 A 内承担"到 B 的出口"，Y 在 B 内同理，两者必须先成为本网可信成员。路由在双向进行对称宣告：X 在 A 宣告"本节点可达 B 网"，本地路由表写入"发往 B→链路对端 Y"；Y 在 B 内对称处理，缺一侧即产生单向黑洞。

信任只在链路上结算：Y 宣告的 B 网汇总，X 用本地导入的信任锚验签，通过才装入路由表，否则拒绝——链路物理层上通、逻辑层上不通。X 无需在 B 内被信任，Y 亦无需在 A 内被信任。

转发与双归属节点一致：A 内包到 X，X 将下一跳指向 Y，Y 按 B 网拓扑继续转发。链路断开时 X 撤销"B 可达"宣告，A、B 各自独立，分裂仅发生在该互联层。同理，可以进行冗余配置。

---

抽象上看两者等价：一个逻辑边界，含 A 侧端点与 B 侧端点，端点之间只做两件事——交换汇总路由、信任校验。区别仅在于端点是否同机：第一种两端点同居一台软路由（内部逻辑链路），第二种分居两台机器（物理网线）。

因此"同级边界"可实现为一个可配置的两端点组件，一套代码覆盖两种形态；一台多网口机器可同时承载多条同级边界。

### 2.3. 组网的硬件需求

由此得到 mesh 节点的准入条件：只要能转发数据包、能通过软件修改路由表，即可参与组网。转发基于查表，无需专用硬件；拓扑由 UDP 广播建立，协议栈均为软件实现；边界只是两端点组件。普通 x86/ARM 设备、软路由、双网口 PC 均可作为 mesh 节点。而末端设备的门槛更低，仅需实现 SLAAC，由邻居设备作为默认网关，信任由边缘节点代持。证书、信任、时间同步均为软件层实现，不依赖特定硬件或内核改动。

## 3. 对上级层级的打包

打包指多个下级节点对外汇总为一个节点，是逐级汇总、单向下行的落地方式。汇总有三种实现。其一是 NAT，边界节点把内部地址翻译为单一外部表象，内部细节对外不可见，外部只见一个地址。其二是前缀长度分配，下级多个子网按层级归并为更短前缀，多个 /64 汇成一个 /60，多个 /60 再汇成 /56、/48，每条汇总路由覆盖下级全部明细。其三是多条前缀并列，打包区同时宣告若干条前缀，任意边缘节点都能处理其中任一条。三种方式都使下级对外表现为一个节点，区别只在前缀形态。

打包后，上级视角中本层只是一个节点，内部子网增减与节点震荡均不外泄，拓扑复杂度随之降低。传入流量可从任意边缘节点进入，无单点入口；前缀与边缘节点解耦，流量落在哪条前缀、从哪个边缘节点进入，内部都能正确投递。流量分布均衡，单个边缘节点失效不阻断进入，与同级边界的并行冗余、路由卸载一脉相承。

## 4. 路由卸载（可选）

路由卸载是可选功能，用于减轻边缘节点的路由负担。当边缘节点承载过重、旁边有设备愿意承担路由业务时，可将一部分路由流量卸载到该对端设备上；对端设备接管相应前缀的转发，本节点相应减少参与。

卸载成立后，邻居节点广播对端路由，把被卸载前缀的下一跳指向对端设备，其余节点据此重算转发路径。对端设备须先成为本层可信成员、进入安全路由表，才能接管转发，卸载不降低安全标准。卸载可按前缀或按流量划分，对端设备可随时退出，退出时撤销对端路由宣告，本节点恢复承载。

## 5. 无线设备的高容忍路由

无线链路丢包、抖动、睡眠与干扰是常态，路由、信任与时间判定须对无线设备高容忍，避免正常波动引发拓扑与信任震荡。容忍只针对无线设备的正常起伏，不放松验签与信任判定本身——UNVERIFIED 依旧不进安全表，转发仍只走可信路径。

失联判定放宽。无线节点须连续多次未收到 Hello 才判定失效，Dead Interval 显著长于有线；短暂睡眠与瞬时干扰不删除邻居、不触发 SPF 重算。无线震荡留在本层，遵循故障不跨区（readme 2.4）。

路由度量平滑。RSSI、丢包率等链路质量信号做滑动平均并带滞回，单次波动不引发重算；稳定路径优先于瞬时最优。度量每 60 秒按 Hello 包重算一次：有线 1 秒 Hello 取 60 个样本，无线 10 秒 Hello 取 6 个样本，窗口足够平滑单跳抖动，又能跟上中长期质量变化。无线链路的默认度量设得很高，除非是无线网桥这类高可靠性设备——度量抬高使大部分连接自然走有线，无线只作有线不可达时的兜底，避免无线成为常态路径。

信任与时间容忍。无线节点的验证计时器与断连重验证设宽限期，短暂断连不清除 TRUSTED；时间样本按无线口径做延迟补偿与剔除（《分布式时钟实现》1.3、1.4），收敛要求放宽，不因无线抖动误降级。
