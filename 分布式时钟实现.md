# SlimeNet-IPv6 分布式时钟实现

> 时钟是底层机制，与《底层实现》平级，安全层是消费者。时间戳载荷搭在《底层实现》的 L3 UDP 广播（Hello）上，由所有 mesh 节点从开机起独立维护，不依赖信任机制存在。
>
> 内容规划分四块：子网内时钟同步、跨子网与跨层时钟校准、TAM 时间接受掩码、时间降级链。前三块见第 1、2、3 节，时间降级链后续补充。

## 1. 子网内时钟同步

范围是单个广播域（一层子网）内的 mesh 节点之间。末端设备零参与、不广播、不贡献时间样本。双归属节点分属两个子网，各跑一个独立时钟实例。

### 1.1. 时钟载荷（搭在 UDP Hello 广播上）

时间戳载荷替代 readme 的 0xDD TLV，字段直接并入《底层实现》的广播载荷；单播地址的定向包不携带时间载荷，降低开销。载荷共五个字段。Clock_Source 占 1 字节，标记时钟源类型：0 为 RTC 硬件，1 为启动晶振，2 为 GPS 备份，3 为人工设定。Epoch_Seconds 占 8 字节，为自 1970-01-01 UTC 起的秒数，64 位无符号整数。Nano_Seconds 占 4 字节，为秒的小数部分，单位纳秒。Confidence_Interval 占 4 字节，为本节点对该时间戳的置信区间（±微秒数），供加权误差预算。Local_Sequence 占 3 字节，为发送递增序号，防重放。

广播间隔与 Hello 一致（有线 1 秒、无线 10 秒），TTL=1 不跨广播域。载荷为强制字段，所有 mesh 节点必须携带。

### 1.2. 本地时钟模型

每个节点维护一个本地时间值，64 位秒加 32 位纳秒，PTP 兼容格式，初始化为 RTC 值；无 RTC 时以上电启动时钟（1970-01-01 起算的相对时间）为初值。时钟单调推进，由本地高精度晶振驱动；对网络校准只接受小幅修正，拒绝大步跳变，防止被恶意或异常样本干扰。修正仅经 1.4 的加权平均产生，不直接采用单个样本。

### 1.3. 样本收集与延迟补偿

每收到一个 Hello 广播，提取邻居时间、序号、置信区间、时钟源与发送者身份作为一个样本，样本的有效性依赖 1.6 的信任联动判定。

延迟补偿是工程要点。广播是单向的，收不到回程确认，无法直接测 RTT；而单跳广播域内传播延迟远小于收敛目标（±50 毫秒）。有线默认补偿为零，无线默认为典型单向延迟（如 2~5 毫秒）；也可对已发现邻居做 UDP 单播 echo 校准，取最近 10 次 RTT 中位数的一半作补偿。补偿不足只影响收敛精度（毫秒级），不影响机制正确性。

### 1.4. 加权平均收敛算法

每收到一轮有效样本后，按加权平均更新本地时钟：

```
T_local_new = (W_self × T_local + Σ(W_i × (T_neighbor_i − Δ_i))) / (W_self + ΣW_i)
```

权重来自安全路由表状态。持时钟证书的强时间源取 50 到 100，按精度等级取权（见 1.10），充当时间强锚。普通 TRUSTED 节点取 10，主导校准。TRUSTED_PENDING_TIME 节点证书通过但时间偏差大，样本本就不可信，取 1，并与 1.6 的剔除构成双保险。UNVERIFIED 节点取 1，可被看见但权重极低。BLOCKED 节点取 0，完全不参与计算。本节点自权重取 5，可配置（readme 5.2 `self-weight`）。

物理观测时间源系统误差大，不进高频平滑权重，作低频绝对锚使用，见 1.11。

异常值剔除：单样本偏差超过 ±5 秒直接丢弃，并将该邻居临时降级为 UNVERIFIED 并告警。恶意节点无法靠注入虚假时间污染全网基准。

### 1.5. 收敛性

每层 256 设备上限，每轮最多收 255 个样本，每次更新开销为线性，计算量可忽略。有线 1 秒间隔下，10 到 15 个 Hello 周期（约 15 秒）内全网收敛至 ±50 毫秒，满足证书校验（±5 分钟窗口）与阈值判定需求。收敛后仍持续校正，补偿时钟漂移，权重保持 TRUSTED 主导。

### 1.6. 与信任状态的联动

时间偏差是信任状态机的外部输入，与安全层 1.5 引用本文判定口径。TRUSTED 节点时间戳连续 3 次跳变超过 ±2 秒，怀疑私钥或 RTC 被篡改，降级为 UNVERIFIED 并触发安全告警。某样本偏差超过 ±5 秒，丢弃样本，邻居临时降级为 UNVERIFIED。收到时间戳早于本地最近一次重启时间的 Hello，判定为典型重放攻击，置为 BLOCKED 并向本层广播告警。

Seq 防重放：接收端要求同源 Local_Sequence 单调递增，否则丢弃。节点重启后 Seq 归零属正常，判定需容忍"Seq 回落 + 时间戳新鲜（晚于对端启动时间）"组合，避免误杀重启邻居。

### 1.7. 阈值滞回体系

时间阈值体系由严到宽共四档。±1 秒为 TRUSTED 升级门槛，时间偏差小于此值才可升级，门槛最严。±2 秒为 TRUSTED 降级触发，连续 3 次超此值即降级。±5 秒为样本剔除门槛，单样本超此值即丢弃并临时降级。±5 分钟为证书有效期窗口，宽松容忍，防微漂误判。

升级门槛严于降级触发，偏差落在两者之间维持现状，形成滞回；±5 秒与 ±5 分钟不参与信任状态迁移。

### 1.8. 冷启动引导

完全断电重启、全网丢失绝对时间时的启动顺序。管理员对参考节点（如边界出口）带外执行 `slimenet clock set 2026-08-28 12:00:00`，该节点成为区域初始时间基准锚。无任何手动设定时，各节点以上电启动时间作相对基准，绝对时效校验降级为相对序列号先后校验（见安全层 1.5 时间降级）。任一节点经外部渠道（GPS、短波授时、手机）获得绝对时间后广播，全网经 1.4 加权平均重新收敛至绝对基准。

### 1.9. 边界与范围

时钟同步只在 mesh 节点间进行，末端不参与，其流量信任由边缘代持，不涉及时效判定。单广播域内 TTL=1，时钟偏差不向相邻子网传播，跨子网时间关系经第 2 节的误差翻译表处理。双归属节点在 A、B 两个子网各跑一个时钟实例，两侧独立收敛，两侧均可作本子网的初始时间锚。

### 1.10. 高可信时钟（证书背书的强时间源）

某些节点携带高精度时基（OCXO、原子钟、GPS 驯服钟），通过特别签发的时钟证书背书，在加权平均中取得远超普通 TRUSTED 的权重，充当子网的强时间锚。

#### 时钟证书

时钟证书由本区域 CA 特别签发，或由既有组织证书加时钟精度扩展而成，在 1.1 载荷基础上附加声明。时基类型标记 OCXO、原子钟或 GPSDO。精度等级为长期漂移率，如 ±1e-9。校准时间为最近一次绝对时间校准的时间戳。有效期为精度保证有效期——漂移会累积，需定期复核或重签。

证书与普通节点证书同一信任体系，伪造需 CA 私钥，非 CA 不可伪造。

#### 权重

持有效时钟证书的节点，样本权重按精度等级取 50（OCXO）到 100（原子钟、GPSDO），为权重表最高档。一个高可信时钟即可显著主导全网收敛，更快达成 ±50 毫秒目标，多数节点普通晶振漂移时维持长期准确。

#### 监督

高权重也意味着一旦被攻破，对网络整体影响巨大；因此采取更为严格的监督。连续 2 次跳变超过 ±2 秒即取消高权重并降级告警，普通 TRUSTED 是 3 次。证书过期或吊销时自动回落普通 TRUSTED（或 UNVERIFIED），权重同步回落。

#### 作为区域时间锚

冷启动或无绝对时间场景下，高可信时钟节点天然是 1.8 之外外部渠道绝对时间的优质来源，可替代手动 `clock set` 成为区域初始时间基准。L0 全明文模式无证书体系，高可信时钟由管理员直接配置指定（如 `slimenet clock anchor <node>`），机制仍可生效。

### 1.11. 互补时间源：物理观测低漂移设备（日晷/月相/星历）

日晷，月相、星历（恒星观测）、六分仪等设备，虽然系统误差大，但是无法伪造且不会累积误差，在安全和粗时钟领域有重要作用。这里定义一个设备类别：以物理观测为基准、零累计漂移的低漂移设备。共性是读数粗糙，系统误差在分钟级，取决于观测手段，但每次观测独立取自物理现实（太阳、月亮、恒星位置），不随时间累积漂移。CA 背书其观测类型与误差声明，纳入安全框架。

#### 证书特性

证书与 1.10 同格式，时基类型为物理观测。时基类型标记日晷、月相、星历、六分仪等观测手段。系统误差为观测偏差声明，如 ±15 分钟。累计误差为 0，无漂移，逐次读数独立。校准时间为最近一次读数时间。

#### 在加权平均中的角色

物理观测样本不进入 1.4 的高频加权平均——粗读数（±分钟级）直接入平滑会污染 OCXO 的毫秒级收敛。它作为低频绝对校正：当网络时间累计偏移接近其系统误差边界（如 ±10 分钟），或按固定低频（每日），用观测读数执行一次绝对时间重锚，同 1.8 的外部绝对时间源，但持续可用、CA 背书。

与高精度源形成互补闭环：OCXO 保短期平滑（低系统误差），物理观测源保长期真实（零累计误差），单一高精度源静默漂移的盲区由观测源兜底。

#### 监督联动

物理观测读数是物理真值的持续来源。若网络时间与观测读数偏差持续逼近其声明的系统误差上限，判定时钟体系漂移超标，触发告警与强制重校准，堵住所有节点一起漂而无人察觉的盲区，是 readme 3.4 物理不可伪造性在长期尺度上的延伸。伪造观测会被一致性监督捕获：读数与网络时间偏差超出其声明误差范围即视作不可信并剥权。

#### 时间间隔验证

对观测源的防污染不能只比对网络时间：全网集体漂移时网络时间本身已不可信，攻击者把伪造读数校准到漂移后的时刻即可混过一致性监督。物理观测另有与网络时钟无关的判据，即相邻读数的间隔。真实日晷随太阳连续推进，两次读数之差应约等于两次观测之间经过的实际时长；按小时级精度与采样频率，每次读数应当比上一次推进约一小时。若相邻读数出现数小时的跃变（如半小时内从 14 时跳到 20 时），该读数物理上不可能，直接判为污染，剥权并告警。间隔验证只依赖观测源自身的相邻读数，不参照任何网络时钟，全网集体漂移的极端情形下依然有效。

#### 与高可信时钟的协作

同网并存时，OCXO/原子钟主导平滑，物理观测源约束长期漂移，双方读数持续互证，矛盾则告警。冷启动时物理观测源可替代手动 `clock set`，且比人工设定更权威——证书背书加持续可用。

## 2. 跨子网与跨层时钟校准（误差翻译表）

子网间不强制同步。每个子网按第 1 节独立收敛自身时钟，时间偏差不跨广播域传播，任何节点都不采信对端时钟作为本地基准。跨子网的时间关系以误差翻译表的形式提供，其信任即 readme 4.8 的 CA 交叉认证本身：对端域时钟由对端 CA 背书，时钟可信度与 CA 可信度等同，不构成叠加其上的独立信任机制。

跨子网时间的用途主要是判定对端 CA 签发的证书是否仍在有效期内。SPF 计算本身不依赖绝对时间，但路由信任依赖：跨组织的节点须先验证证书有效期才能入安全表、成为中转跳。路由对时间的依赖经由信任层间接存在，跨子网时间判定直接决定跨组织路由是否可用。

### 2.1. 误差翻译表

翻译表以对端子网为键，记录对端 CA 交叉认证身份、对端参考时间相对本子网参考时间的偏移量，以及该偏移的有效窗口与置信度。用途是时间平移：本子网内的时效判定（证书有效期、防重放、吊销指令签发时间）一律以本地参考时间执行，涉及对端时间戳时先按表项平移成本地参考，再走标准判定口径（TAM、±5 分钟窗口）。平移只发生在判定环节，对端时钟从不进入本子网的加权平均，也不被本子网节点采纳为本地时间。

### 2.2. 建立在 CA 交叉认证之上

翻译表不是平级协商的产物，而是交叉认证的副产品。两子网经同级边界互联、双方 CA 完成交叉认证后，边界节点交换各自子网的参考时间基准，生成翻译表项。只有交叉认证的对端才有权注入表项，交换内容经签名校验；对端 CA 证书吊销时，对应表项随之失效。这与汇总路由的信任模型同源——没有交叉认证，就没有路由，也没有时间平移。

### 2.3. 动态时间询问

翻译表项也可按需生成。判定对端某证书有效期前，本侧沿已信任的跨域路径，向对端 CA 域内的一台可信设备（交叉认证通过、已在本侧安全表的节点）发送时间请求，对方以当前参考时间应答，应答经签名校验。

应答由被查询端点以其交叉认证身份签名，端到端不可伪造。中间节点只能转发或丢弃，无法篡改或替换，CA 交换制度下中间人攻击无从实施。对端域的时钟证书由该区域 CA 签发，时钟可信度与 CA 可信度等同，时间样本的真实性只依赖端点身份，与中继节点无关。因此询问路径上的中继节点不必逐一交叉认证，只有应答端点须落在已交叉认证的信任锚上。中继至多造成丢包，属可用性问题，不影响样本正确性。

总传输延迟即该样本的误差范围：应答到达时，对端真实时间落在应答时间戳加减全程延迟之内。判定时把该误差并入有效期窗口，本地 ±5 分钟窗口外再叠加传输延迟预算；有线点对点链路延迟为毫秒级，卫星等长链路达秒级，相对窗口均可忽略。询问只在需要时发起，不产生持续开销。

### 2.4. 老化与降级

各子网独立漂移、独立校准，相互偏移随时间缓慢变化，翻译表项可经边界定期刷新，也可按 2.3 按需询问取得新鲜样本。表项超出有效窗口或置信度下限时，跨子网时效判定不再依赖绝对平移，降级到宽松口径，改用证书序列号相对先后、放宽有效期窗口。降级只影响跨子网判定，不触及各子网内部时钟。

### 2.5. 组合与跨层

间接相邻的子网（A—B—C 链）可经多级偏移复合平移，但误差逐级累加，只作兜底；直连交叉认证的子网才持有直接表项。跨层与跨子网是同一机制：层级划分中的每一层都是独立广播域，上下层边界（BR）处按同一方式建立翻译表，楼栋层不感知区域层内部时钟，反之亦然。

## 3. TAM 时间接受掩码

TAM 是时钟区间认证的手动实现。物理观测锚（1.11）以证书背书自动提供物理可信的时间区间，TAM 则是其手工版本：操作者用表达式规定什么时间在物理上可接受，收包时把时间戳代入表达式求值，命中接受区间才放行。

### 3.1. 表达式配置

TAM 以表达式配置，默认采用 Python 语法，但这是软件实现，不局限于 Python，可替换为 Lua、JavaScript 或内嵌 DSL。表达式接收时间戳，先经换算函数转为统一基准，unix 秒/毫秒、yymmddhhmmss、年月日等格式互相转换，再与预设时间或本机时间比较，返回真或假。换算与比较由实现提供的函数承担，表达式只组合判断逻辑。

```python
def accept(ts):
    t = to_local(ts, tz="+8")   # 任意格式换算成本地时间
    return in_window(t, "2026-08-28 14:35:00", "2026-08-28 14:39:00")
```

实现不采用 readme 3.4 的 Cron 掩码语法，改用表达式求值，覆盖相同场景且更灵活。

### 3.2. 判定基准与误差

比较基准二选一：预设时间或本机时间。预设时间用于冷启动无绝对时间的场景，本机时间用于正常状态。接受区间的宽度由表达式显式给出，落在区间外的一律拒绝。区间越窄抗重放越强，越宽生存性越高，粒度权衡与 readme 3.4 一致；未命中时沿用其求生模式，逐级放宽区间直至完全信任。

### 3.3. 与物理观测锚的关系

TAM 与物理观测锚（1.11）是同一机制的两副面孔：TAM 手工配置，观测锚经 CA 背书自动提供区间（安全层 1.6）。已配置观测锚的子网可用其区间取代手工表达式，免去人工维护。
